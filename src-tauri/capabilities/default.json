{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main popup window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
